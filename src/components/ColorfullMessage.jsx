import React from "react";

const ColorfullMessage = () => {
  const contentStyle = {
    backgroundColor: "pink",
    fontSize: "18px"
  };

  return <p style={contentStyle}> テストっすね</p>;
};

export default ColorfullMessage;
